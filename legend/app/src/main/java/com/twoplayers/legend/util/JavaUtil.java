package com.twoplayers.legend.util;

import java.util.ArrayList;

public class JavaUtil {

    /**
     * Add a element Twice in a list
     */
    public static void addTwice(ArrayList list, Object element) {
        list.add(element);
        list.add(element);
    }
}
